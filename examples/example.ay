#use "basic";

struct Person {
    name: string,
    age: s32
}

func print_person(p: *Person) {
    printf("%.*s %d\n", p.name.length, p.name.data, p.age);
}

func main() s32 {
    p: *Person;
	using p;

	p.age = 510;
	age = 31;

	/*
    p.age = 240;
    
	print_person(&p);*/

    return 0;
}
